%%
deltaFracDeg = 0.001;

baseLat = -90:deltaFracDeg:90;

baseLong = -180:deltaFracDeg:180;

posCount = 1;

coordPair(1,1) = [0,0]
%%
for outerCount = 1:length(baseLat)

    for innerCount = 1:length(baseLong)
    
    coordPair(posCount, posCount) = [baseLat(outerCount), baseLong(innerCount)];    
        
    end
    
end

%%
baseLat(1)

testArray(1) = 34
testArray(2) = 365346